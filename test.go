package main

// sample usage
func main() {
	sumTables:=0
	Locations:= []int{0,1}  ;
	for i := 0; i < len(Locations); i++ {
		for j := 0; j < Locations[i]; j++ {

		}
		sumTables += Locations[i]
	}
}
